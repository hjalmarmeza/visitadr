rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite lectura y escritura de forma permanente para el MVP
      allow read, write: if true;
    }
  }
}
